# Build artifacts and backups
*.zip
FinCalcHub_Site_v*

# Source documents (not for deploy)
*.pdf
*.docx

# Stale / mystery files
test_write.txt
zi6GQwzV
zifB24pq
site/

# Local keyword-research artefacts (not for deploy)
seo_keyword_research/

# OS / IDE
.DS_Store
Thumbs.db
.idea/
.vscode/

# Python (defensive — venv lives one dir up, but just in case)
.venv/
__pycache__/
*.pyc

# Node
node_modules/

# Credentials & secrets — NEVER commit (added 2026-06-10)
.env
*.env.bak
.gsc_service_account.json
.gsc_oauth_client.json
.gsc_token.json
*service_account*.json
*oauth_client*.json
*.key
*.pem

# Local agent tooling, runtime logs & state
.claude/
*.log
*.jsonl
logs/kpi_etl_state.json

# Stray dump files
*intros_dump.txt
